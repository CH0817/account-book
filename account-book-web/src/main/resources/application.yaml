server:
  servlet:
    context-path: /account-book
spring:
  profiles:
    active: @profileActive@
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: trace